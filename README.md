first on command prompt wrote cd desktop
cd PLPBasicGitAssignment
git init command
created text file( hello.txt)
git add hello.txt
git commit -m "add"
git remote add origin (PLPBasicGitAssignmentURL)
git push -u orign master
